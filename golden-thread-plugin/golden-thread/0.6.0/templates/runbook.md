# {{TITLE}} — Runbook

<!--
  THE INCUBATOR.

  This is the cheap capture point for facts about WORKING IN THIS CODE — the real
  command, the environment trick, the structural landmine, the thing that cost an
  hour because nobody wrote it down.

  Write here the moment you learn something. No ceremony, no PR, no review. A fact
  that is still changing belongs here and nowhere else.

  WHY IT EXISTS SEPARATELY FROM CLAUDE.md: the graduation trigger is STABILITY — a
  fact that survived several sessions unchanged is safe to publish. Stability can
  only be observed somewhere a fact sits and is watched. This is that place. Without
  it, CLAUDE.md has a destination but no on-ramp, and every entry has to be authored
  deliberately instead of accumulating.

  WHAT DOES NOT GO HERE:
    - Process rules that apply across projects  -> PROTOCOL.md
    - Architecture                              -> design.md
    - Why a choice was made                     -> decisions.md
    - Dated findings and incidents              -> research.md

  GRADUATION: when an entry has been stable across several sessions AND passes the
  teammate test ("would this make sense to someone who has never heard of this
  vault?"), move it into CLAUDE.md and RETIRE THE LINE HERE. The fact moves; it does
  not get copied. Log it with `graduate`.

  Duplication across two projects' runbooks is a strong signal the fact belongs one
  layer out — /gt:gt-runbook-lint looks for exactly that.
-->

## Commands

<!-- The ones that actually work, not the ones the docs claim. -->

| Do this | Command |
|---|---|
| | |

## Environment

<!-- Setup steps, versions that matter, the thing that has to be running first. -->

## Landmines

<!-- Format: one line, dated. Add to the bottom; that is what makes stability visible.

     - 2026-08-16 — <the fact>. <why it bites>.
-->

## Graduated

<!-- Entries that have moved out to CLAUDE.md. Keep the one-line stub with a date so
     the history stays readable and nobody re-adds a fact that already left.

     - 2026-08-16 — <fact> -> CLAUDE.md
-->
