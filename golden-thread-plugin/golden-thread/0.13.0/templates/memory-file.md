---
name: <kebab-case-slug>
description: "<one line — used to decide whether this file is worth opening>"
metadata:
  node_type: memory
  type: feedback          # core | feedback | user | reference
  level: generic          # core | context | generic   (default: generic)
  enforcement:            # validated | reminder — required iff level: core, omit for generic
---

<The fact, rule, or finding. One statement per rule, phrased as an imperative.>

**Tier:** <level> / <enforcement> — see [[core_rule_priority_model]].

**Why:** <what happened that makes this worth keeping; a tier is justified, not asserted>

**How to apply:** <what to actually do differently>
