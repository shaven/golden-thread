# macOS
.DS_Store

# Build/distribution artifacts — regenerate with golden-thread-plugin/install.sh
*.zip

# Marker Claude Code writes into the installed plugin cache, not source
.in_use

# Python bytecode
__pycache__/
*.pyc
